#include "Solver.hpp"

      Solver::Solver        (std::vector<Volume *>* _objects)
{
  objects = _objects;
}

void  Solver::initParticles (int N_init)
{
  int i;
  for(i = 0; i < N_init; i++)
  {
    
    
  }
}
